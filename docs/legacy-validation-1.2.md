# 검증 기록 · 1.2.0

2026-09-22, Windows 64비트 / Python 3.12.14 / CadQuery 2.8.0 / OCCT 7.9.3.

## 자동 시험

**pytest 90개, Node 기하/이력 시험 12개 통과.** FastAPI/Starlette 의존성의 anyio 별칭 폐기 예정 경고 1건이 있으며 기능 실패는 없습니다.

```powershell
python -m pytest -q
node --test tests/sketch-geometry.test.mjs
```

- 9개 기본 설계: 유효한 실제 솔리드, STEP 재불러오기의 솔리드 수·체적, 이진 STL 구조·삼각형 체적.
- 시편의 해석적 체적·목 단면, 관통 구멍, 회전·이동 순서, 정확한 바운딩 박스와 체적 간섭.
- 치수 범위, 구멍 충돌, 자가 교차 스케치, 선언형 스키마 외 입력 거부.
- 스케치 해석: 완전 구속 사각형 20 × 10 × 5 mm = 1,000 mm³, 부분 구속 각도, 자유도, 충돌 치수·잘못된 점 참조 거부.
- 조립: 부모의 치수·관절 각도 변경 시 자식·핀 위치 전파, 회전 구속 2개의 자유도 2, 순환·중복·고정·기준점 충돌 거부.
- 면 스케치: 상면·하면 돌출, 상면·회전한 측면 파내기, 체적 변화와 STEP 재불러오기, 메시 삼각형의 면 대응, 곡면·변경된 기준면 거부.
- 프로젝트 저장·복원·갱신, 다운로드, 허용 호스트·출처·요청 헤더·일반 1 MB / 프로젝트 32 MB 경계.
- AI 모의 시험: 구조화 응답, 오류 교정, 거절, 키 미설정, 공급자 오류의 비밀값 숨김. **실제 유료 OpenAI 요청은 실행하지 않았습니다.**
- Autodesk ZIP: 전체 STEP, 부품별 단일 솔리드 STEP 5개 재불러오기, 원본 JSON의 구속, 변환 스크립트 포함. 가짜 F3D/IPT 확장자를 만들지 않음.

## 1.2 스케치·기록 추가 검증

- 원점 고정 + 직경 변경 시 중심 유지 및 자유도 0, 두 직선의 교점에 구속한 점의 위치 해석.
- 수평/수직·일치·거리·X/Y 거리·각도·반지름·직경·평행·직각·동일·동심·동일 직선·접선·중간점·대칭·점-곡선·곡률 구속 잔차와 Jacobian 순위; 충돌 치수 거부.
- 사각 윤곽 내부 원의 해석적 면적·체적, 구성선 제외, 원/선 교점, 영역 합치기, 떨어진 영역의 복수 솔리드.
- 회전 타원의 정확한 면적, 맞춤점/제어점 닫힌 스플라인 솔리드, 면 기준 변환, 직선·원 경계의 정확한 투영.
- JS: 원/호의 방향과 교점, 슬롯 폐곡선, 사각형 직각, 자르기·연장·분할·간격띄우기·모깎기 접선·대칭, 구형 스케치 변환.
- 작업 이력 47단계 + 분기의 API 저장/불러오기/Autodesk ZIP 보존, 조작된 이전 값·순환 참조·현재 형상 불일치·프로토타입 경로 거부. JS 이력은 50단계 이후 분기 복원.
- 실제 UI: 원점 중심 원 + 지름 20 → 완전 구속. 슬롯 + 두 원 → 영역 3개, 가장 큰 영역 1057.6106 mm². 상면 7의 원점에 지름 8, 깊이 3 파내기. 투영한 6개 경계는 완전 구속. 도구 순서·면 좌표계·기준 피처·구속 참조 표시 및 저장 성공.
- 실제 UI에서 이전 단계 두께 8 → 9 변경 시 7번째 분기가 생기고 기존 구멍 피처 단계로 복원됨. 새로고침 후 이력 보존.

## 브라우저 UI 확인

Codex 브라우저의 실제 클릭·입력으로 확인했습니다.

- 1440 × 900 작업 공간과 작은 브라우저 폭의 반응형 배치.
- 시편 치수 수정, 잘못된 치수 거부, 로컬 명령 초안 미리보기·적용·취소, 자동 복원.
- 조립의 부품 선택·간섭 표시·되돌리기, 프로젝트 저장·다시 열기, 설계 JSON 가져오기, STEP 내보내기.
- 구멍판의 상면을 직접 클릭해 스케치 편집기를 열고 사각형을 그림.
- 고정점·수평·수직·두 거리로 완전 구속(7개 구속, 잔차 0). 동일 변에 10 mm와 30 mm를 동시에 지정하면 충돌 오류, 제거 후 복구.
- 20 × 10 × 5 mm 면 돌출: 구멍판 체적 28,121.42 → 29,121.42 mm³. 같은 스케치를 2 mm 파내기로 변경: 27,721.42 mm³.
- 어깨 관절을 25° → 75°로 수정하면 연결된 두 번째 링크의 월드 각도 −30° → 20°. 구속된 부품의 직접 월드 이동은 잠김.
- 관절 구동 도구로 25° 복원, 조립 구속 4개·고정 1개·자유도 2 표시.
- Autodesk 변환 ZIP 다운로드. 마지막 새로고침 이후 브라우저 오류·경고 로그 없음.

화면: [스케치 도구](docs/sketch-tools.png), [작업 기록](docs/design-history.png), [면 스케치](docs/face-sketch.png), [구속 조립](docs/assembly.png).

## Windows 패키지 확인

PyInstaller로 Python·CAD 커널·WebView 창을 포함한 실행 파일을 만들었습니다.

- `PromptCADStudio.exe --self-test report.json`: 시편·돌출·5부품 조립 생성 및 실제 STEP 저장 성공. 원점/직경이 완전 구속된 원의 해석적 스케치·영역·솔리드 시험도 포함.
- `--smoke-test report.json`: 일반 Windows 사용자 데스크톱에서 EdgeChromium/WebView2 문서 로드 성공, 내장 루프백 API 응답, 자동 종료.
- 브라우저 UI 시험과 독립 실행 파일의 커널/창 로드 시험은 별도입니다. Windows 앱 전체 조작의 네이티브 UI 자동 시험은 수행하지 않았습니다.

## 검증 범위

F3D/IPT 변환 도구는 Autodesk 공식 API를 사용하지만 Fusion/Inventor 내부 실변환은 아직 시험하지 않았습니다. 솔리드 전달용이며 원본 스케치·구속을 Autodesk의 피처 이력으로 재구성하지 않습니다.

CAD 영구 위상 추적, 폐루프 조립, 동역학, 가공성·강도·규격 인증은 검증 대상이 아닙니다. 간섭은 현재 배치의 체적 교차만 확인합니다.
